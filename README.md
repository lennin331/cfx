## Options
- fetch 
    *    fetchs the specified question from the domain
- config 
    *    configure option from local to the domain login
- submit 
    *    submit the specified question to the domain

---

Fetch has the most operations within in, so making the get_id a helper makes sense 

fetch.go 
helper/get_id.go
helper/


> get_id_active : Handles active contest - registeration and live standings and submit status
